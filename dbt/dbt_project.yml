name: 'crypto_pipeline'
version: '1.0.0'
profile: 'crypto_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  crypto_pipeline:
    +materialized: view
    silver:
      +schema: silver
    gold:
      +schema: gold
